# Dependencias del harness de evals (CI y local).
# El retriever baseline solo necesita pyyaml; numpy+fastembed son para
# semantic/hybrid (modelo ONNX local, sin GPU).
PyYAML==6.0.3
numpy==2.5.1
fastembed==0.8.0
